/*
 * Single place the vendored Vue build is referenced from. Everything else
 * imports Vue through here, so bumping the version means dropping a new file
 * into vendor/ and editing one line.
 *
 * This is the *full* build (runtime + template compiler), which is what lets
 * components declare `template:` as a plain string with no build step.
 */
export * from "../vendor/vue.esm-browser.prod.js";
